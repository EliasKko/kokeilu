class hissi:
    def __init__(self, kerros):
        kerros = kerros
    def ylös(self, kerrkset):
        for i in range(kerrokset)
            if
                self.kerros == self.kerros + kerrokset
